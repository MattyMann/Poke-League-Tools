name: Automatically make requirements.txt

on: [push]

jobs:
  build-linux:
    runs-on: ubuntu-latest

    steps:
    - name: Automatic requirements.txt for Python Projects
      uses: ryan-rozario/pipreqs-action@v2.0.0
